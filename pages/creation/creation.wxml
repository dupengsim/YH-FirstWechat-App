<view class='container'>
  <block wx:for="{{cardImgs}}" wx:key="{{item.Id}}">
    <image src='https://www.cmmooc.com{{item.Url}}' catchtap='onCreationTap' data-index="{{item.Id}}"></image>
  </block>
  <text class='toLead_btn' catchtap='onImgUp'>从相册导入图片</text>
</view>